[project]
name = "difflogic2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mlx>=0.29.4",
]

[tool.uv.workspace]
members = [
    "mlx",
]

[dependency-groups]
dev = [
    "ruff>=0.14.5",
]
